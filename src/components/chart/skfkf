            <Line data = {{
                labels: forecastData.list.splice(0, 7).map((item, idx) =>item.main.temp_max),
                datasets:[
                    {
                        label:"Farhan",
                        data: forecastData.list.splice(0, 7).map((item, idx) =>item.main.temp_max),
                        backgroundColor:"red"
                    }
                ]
            }}/> 
